<div class="card">

    <div class="card-header">
        <h4>{{title}}</44>
        <h8>Posted by: {{user.username}} on {{format_date created_at}}</h8>
    </div>

    <div class="card-body">
        <p>{{post_content}}</p>
        <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
    </div>
</div>